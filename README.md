# SDE Assignment - Nov 2025

This module exports mergeTimeRanges(ranges, threshold) function.

Run using:

const myModule = require('./merge-module.js');
console.log(myModule.mergeTimeRanges([...], 200));
